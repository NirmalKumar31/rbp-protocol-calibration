# Dashboard only. Deliberately separate from the study's pyproject.toml so that installing the
# dashboard cannot change the environment the analysis runs in, and so a Streamlit or Plotly
# bump can never alter a published number.
#
# Direct dependencies are pinned exactly, not with >=, so a Streamlit or Plotly bump cannot
# arrive silently. This is NOT a lock file: transitive dependencies resolve freely, and the
# two font families load from a CDN, so it stabilises the stack rather than guaranteeing a
# byte-identical render. A full lock would need hashes and self-hosted fonts.
streamlit==1.40.2
plotly==5.24.1
pandas==2.2.3

# Test and lint only.
pytest==8.3.4
ruff==0.16.3
